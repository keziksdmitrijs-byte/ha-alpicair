# AlpicAir Ventilation Unit

HACS-интеграция Home Assistant для рекуператоров AlpicAir, произведённых SALDA на контроллере MCB 1.27, через Modbus TCP.

## Версия 0.5.0

### Ползунки расхода воздуха

Добавлены восемь изменяемых ползунков для настройки производительности вентиляторов по режимам:

| Направление | Защита здания | Эконом | Комфорт | Форсаж |
|---|---:|---:|---:|---:|
| Приток | 450 | 451 | 452 | 453 |
| Вытяжка | 456 | 457 | 458 | 459 |

Ползунки отображают 0–100%. Контроллер хранит эти значения с масштабом ×0.1%, поэтому 30% записывается как 300, а 1[...]

### Night Cooling

Полностью настраиваемые параметры ночного охлаждения:

- Включение/выключение: Coil 4.
- Час и минута начала: Holding Registers 25–26.
- Час и минута окончания: Holding Registers 27–28.
- Температура вытяжки для запуска и остановки: Registers 29–30.
- Наружная температура для остановки: Register 31.
- Уставка температуры приточного воздуха: Register 32.

Температуры Night Cooling вводятся в °C и автоматически преобразуются в формат ×0.1°C контроллера.

### Надёжность опроса

Необязательные расширенные диапазоны (расходы, фильтры, Night Cooling) опрашиваются отдельно. Если конкретный ди[...]

## Установка через HACS

1. HACS → Integrations → меню с тремя точками → **Custom repositories**.
2. Добавьте `https://github.com/keziksdmitrijs-byte/ha-alpicair`.
3. Категория: **Integration**.
4. Установите **AlpicAir Ventilation Unit**.
5. Перезапустите Home Assistant.
6. Настройки → Устройства и службы → Добавить интеграцию → **AlpicAir**.
7. Укажите IP шлюза, порт (обычно 502) и Slave ID (обычно 1).

## Новая фича: привязка сущностей на картинке (Options / Proxy sensors)

Добавлена возможность сопоставлять позиции на вашей картинке (actual_supply_1..actual_extract_4) с любыми сенсорами в Home Assistant через параметры интеграции (Options).

- Откройте Интеграции → AlpicAir → Параметры (Options) и укажите сущности для каждой позиции (селектор ограничен доменом `sensor`).
- Интеграция создаёт прокси‑сенсоры с устойчивыми `unique_id` вида `{entry_id}_proxy_{key}`. Используйте именно эти прокси‑сущности в Lovelace (picture-elements). При изменении маппинга в параметрах прокси начнут зеркалировать новые сущности — редактировать Lovelace не потребуется.

## Структура репозитория

```text
ha-alpicair/
├── hacs.json
├── README.md
└── custom_components/
    └── alpicair/
        ├── manifest.json
        ├── __init__.py
        ├── config_flow.py
        ├── coordinator.py
        ├── const.py
        ├── number.py
        ├── sensor.py
        ├── switch.py
        ├── button.py
        ├── select.py
        ├── strings.json
        └── translations/
```

## Важно

Не смешивайте эту HACS-интеграцию со старой ручной YAML-конфигурацией того же рекуператора из `packages/modbus.yaml`. Уда[...]
